import {getJson, PondListResponse} from '../../src/api';
import {ErrorPanel} from '../../src/ErrorPanel';
export default async function MapPage(){try{const data=await getJson<PondListResponse>('/ponds?limit=1000'); const located=data.items.filter(p=>p.location); const missing=data.items.length-located.length;return <main className="grid"><section className="card"><h1>全国監視マップ</h1><p>MapLibre GL JSで実座標を扱う設計です。BBox連動検索: /ponds?bbox=minLon,minLat,maxLon,maxLat</p><div><span className="pill">クラスタリング上限1000件</span><span className="pill">欠損座標 {missing}件</span></div><div className="map" data-testid="map">{located.slice(0,1000).map(p=><a key={p.pondId} className="marker" href={`/ponds/${p.pondId}`} style={{left:`${Math.max(0,Math.min(95,((p.location!.coordinates[0]-122)/(154-122))*100))}%`,top:`${Math.max(0,Math.min(95,(1-(p.location!.coordinates[1]-20)/(46-20))*100))}%`}} title={`${p.name} ${p.coordinateQuality}`}>{p.risk?.riskLevel||'未評価'}</a>)}</div></section><aside className="card"><h2>サイドパネル</h2><p>リスクレベルとデータ品質を確認できます。</p></aside></main>}catch(e){return <main className="card"><ErrorPanel error={e} feature="監視マップ"/></main>}}
