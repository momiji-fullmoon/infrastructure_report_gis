# Full import result

Status: not_completed

The repository now includes a manual `full-import` workflow that performs 1,000-row, 10,000-row, full, and rerun imports against PostGIS and uploads JSON artifacts. Do not replace this status with measured values until a successful workflow run has completed with the real Excel workbook available to GitHub Actions.
